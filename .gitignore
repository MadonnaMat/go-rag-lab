.env
/tmp/

# Generated by `make swagger` (also a prerequisite of build/vet/test-unit/
# test) from @-annotations in cmd/serve and internal/api — see CLAUDE.md's
# "API docs" section. Pure build output, never hand-edited, so it isn't
# committed.
/docs/

# Playwright MCP scratch output
.playwright-mcp/
